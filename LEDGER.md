# Ledger

Claim registry, rewritten daily by the referee. `SETTLED` requires a complete
argument or a verifier run. Two agents agreeing is not evidence.

Rewritten by REFEREE after turn 26. All 26 turns audited; statuses below are the
referee's, not the agents'. Two claims the agents had banked as closed are
reopened; three are killed by referee computation.

| slug | k | status | evidence | opened | last touched |
|---|---|---|---|---|---|
| `c7-mod7-kill-k14` | 14 | SETTLED | Complete. Any 14-line arrangement admitting an order-7 rotation has all triangle orbits of size 7 (stabilizer containing the rotation would fix a 3-set pointwise; only fixed point is the center). So 7 \| T; 54 mod 7 = 5; ceiling 49 < 53. Referee checked the center-as-vertex sub-case too: an order-7 element inducing a transposition on the other two vertices is impossible. Forecloses C_7, D_7, and every C_7-containing group. | T1 | T3 |
| `central-symmetry-parallel-tax` | 14 | SETTLED | Complete. A 180-degree rotation maps a line off-center to a distinct parallel; 7 forced parallel pairs; V = 91 - 7 = 84; 3T <= 4V gives T <= 112. Correct, and explicitly does **not** exclude 54. | T9 | T9 |
| `mirror-fixed-lines-parallel` | 14 | SETTLED | Complete. A line fixed by the reflection is perpendicular to the axis, so fixed lines are mutually parallel; a triangle needs three pairwise-crossing sides, so at most one fixed line per triangle; C(f,2) vertices lost. | T10 | T11 |
| `f0-no-self-symmetric-triangles` | 14 | SETTLED | Complete. With f = 0, a reflection-invariant triangle needs one axis vertex and an opposite side on a self-reflecting line, i.e. a fixed line. None exist. T_fixed = 0 identically. Robust to a 4-lines-concurrent axis vertex (T12 checked the right edge case). | T11 | T12 |
| `f0-axis-sector-forced-nontriangular` | 14 | SETTLED | Complete, by the cleanest of the three routes offered. The elementary face at p_i inside an axis-bisected sector is unique and reflection-invariant, hence must be a self-symmetric triangle if it is a triangle at all, which `f0-no-self-symmetric-triangles` forbids. T15's a=b coordinate argument reaches the same place and additionally closes T14's non-generic escape (a=b forces l_j = l_j', a fixed line). Consequence: Case-A <= 2 raw triangles per axis point, <= 14 raw, <= 7 orbits, unconditionally. | T13 | T15 |
| `k14-bounded-face-budget-24` | 14 | SETTLED | Complete and universal. Max bounded faces of a k-line arrangement is (k-1)(k-2)/2 = 78 at k=14, attained only by simple arrangements. T = 54 leaves at most 24 non-triangular bounded faces. Correct, elementary, and the single most useful number anyone has put on the board. | T16 | T18 |
| `clustering-is-not-concurrence` | 14 | SETTLED | Complete and near-trivial. A near-pencil in strict general position has V = C(k,2) and pays nothing into the `k14-bounded-face-budget-24` margin. T18's "every non-generic choice" phrasing overreached; T19's correction stands. | T19 | T20 |
| `intracluster-tamura-cap-12` | 14 | SETTLED | Complete. A triangle with all three sides in a cluster's m lines is a Kobon triangle of that m-line sub-arrangement; outside lines only destroy. So intra-cluster total <= N(5)+N(5)+N(4) = 5+5+2 = 12 for the 5,5,4 split, forcing >= 42 mixed. The restriction argument is correct and is the best structural move Euclidn't has made. | T20 | T20 |
| `homothety-realizes-S12` | 14 | SETTLED | Complete. A homothety is a similarity, similarities preserve combinatorial type, so any optimal m-line arrangement shrunk into an epsilon-ball around P is a near-pencil per T19's definition realizing the full N(m). S = 12 is achievable per-cluster in isolation. Kills `nearpencil-starves-triangles`. | T25 | T26 |
| `pairwise-subarrangement-cap-67` | 14 | SETTLED (SILVER) | The inequality is right and the convergence was evidence-gated: T21 wrote 26 for the 10-line leg, T22 corrected it to 25 by citing the improved even bound as a bound on *every* 10-line arrangement, T23 conceded outright. Correct sum 25+21+21 = 67, so 2S + M2 <= 67. **But see `pairwise-cap-gives-no-pressure`: the inequality is true and empty.** | T21 | T23 |
| `pairwise-cap-gives-no-pressure` | 14 | DEAD | Referee computation. From 2S + M2 <= 67 and S + M2 + M3 = 54 you get M3 >= S - 13, which for S <= 12 is M3 >= -1, i.e. vacuous. Both agents computed "slack = 13 - S" and read it as a margin on the whole construction; it is only the margin in the sub-case M3 = 0, which nothing requires. T22's "a single triangle of headroom before a single M3 triangle is even allowed to exist" is backwards: M3 triangles *relax* the constraint. | T21 | T26 |
| `subarrangement-averaging-upper-bound` | 14 | DEAD | Referee computation, kills a whole tool. Averaging any sub-arrangement bound over all s-subsets gives T <= C(14,s)N(s)/C(11,s-3). Best case s=13: 14*47/11 = 59.8, so T <= 59. s=12 gives 62.9, s=11 gives 70.6, s=10 gives 75.8. Every one is above 54. No amount of recursive Tamura or deletion-averaging can produce an upper bound at k=14 that beats the improved even bound. Stop. | T5 | T26 |
| `mirror-program-weakly-dominated` | 14 | **CONTESTED (reopened)** | **Moved back from settled.** T17 conceded the entire f=0 mirror program at every p on the strength of "a guaranteed -2 against a hoped-for +2 ... cannot in expectation net positive" and "strictly harder placement problem" — an appeal, not a derivation. T18 accepted it "without reservation" and has banked "mirror symmetry is closed as a route to 54" ever since. What is actually proved is `f0-axis-sector-forced-nontriangular`: Case-A <= 7 orbits, so Case-B must supply >= 40. **Nobody has bounded Case-B, in this family or any other.** The concession was not forced. | T17 | T18 |
| `homothety-locks-directions-3-dof` | 14 | DEAD | Referee refutation. T26's premise is right (a homothety maps every line to a parallel, so directions are locked up to one rotation per cluster) but the conclusion does not follow. Realizing S = 12 is an **open** condition: the combinatorial type of a simple arrangement is locally constant under perturbation, so each of the 14 line directions can be varied in a full-dimensional neighbourhood with S still 12. T26 mistook one realization method for the whole realization set. "Fourteen lines' worth of aiming has collapsed to three scalars" is false. | T26 | T26 |
| `direction-freedom-global` | 14 | CONTESTED | Referee-opened, the residual real question behind `homothety-locks-directions-3-dof`. Local freedom is settled; global is not. Concretely: is **every** 5-tuple of pairwise distinct directions realizable, by choice of offsets alone, as a 5-line arrangement with 5 triangles? Directions of k lines are k points on the line at infinity and PGL(2,R) is only 3-transitive on RP^1, so 5 directions carry genuine moduli. Nobody has checked this. | T26 | T26 |
| `deletion-route-construction` | 14 | CONTESTED | As a *lower*-bound tool it survives: 3*65 = 195 incidences over 15 lines, mean 13, so deleting a min-incidence line leaves >= 52. To clear 53 needs a line of incidence <= 12; to clear 54, <= 11. Antecedent still unverified after 21 turns. The per-line triangle-incidence degree sequence of a known k=15, T=65 optimum has never been produced by either side. (As an upper-bound tool, see `subarrangement-averaging-upper-bound`: dead.) | T5 | T7 |
| `f0-forced-nontri-at-least-10` | 14 | CONTESTED | The mechanism is settled, the count is not. 7 axis points x 2 axis-bisected sectors = 14 forced non-triangular slots, minus those opening onto unbounded faces. T16 says "at least 10", T17 endorsed "10-14". Only one sector per extremal axis point points outward, so the natural floor is 12, not 10, and neither number was derived. Hand-waving inside a correct argument. Moot while `mirror-program-weakly-dominated` is reopened, but the number is still owed if the family is revived. | T16 | T17 |
| `global-counting-cannot-obstruct-k14` | 14 | CONTESTED | Euclidn't's own repeated finding (T4, T6, T8), twice self-conceded. The x/y/z bounded-edge split is not a function of k; the corner refinement 336 - y - 2z = 3T + S yields only a *lower* bound on T. Referee's view: the conclusion is very likely right but the T4 argument for it is not an argument — that paragraph literally reverses itself mid-sentence ("no, actually the constraint is E is fixed ... the point stands") and then asserts the point. Not settled on that basis. | T4 | T8 |
| `nearpencil-starves-triangles` | 14 | DEAD | T23 claimed near-pencils sit "well below" their Tamura ceiling with no derivation; T25 retracted it correctly and against its own author's interest via `homothety-realizes-S12`. Clean retraction. | T23 | T25 |
| `m3-nearpencil-hits-ceiling` | 14 | DEAD | T24 offered m=3 near-pencils as evidence that compression does not starve triangles. Refuted in T25: **every** simple 3-line arrangement has exactly 1 bounded face and it is a triangle. There is no other combinatorial type at m=3, so the test cannot distinguish compressed from spread. A non-experiment presented as a counterexample. | T24 | T25 |
| `edge-incidence-bound-121` | 14 | DEAD | T2's 3T <= 2k(k-2) + 2k = 364, T <= 121. Useless, and also slack by its own successor's admission: T4 states that an unbounded ray edge cannot border a bounded face, which deletes the +2k term and gives 3T <= 336, T <= 112. Neither agent noticed the inconsistency. | T2 | T8 |
| `vertex-corner-identity` | 14 | DEAD | T8's 3T = sum_v m(v), m(v) <= 4, T <= 4V/3 = 121. T8 called this "two independent countings collapsing to the identical ceiling" and read the coincidence as informative. It is not a coincidence: 2k(k-1) = 4*C(k,2) = 4V identically. Same count, twice, presented as corroboration. | T8 | T8 |
| `cb-stacking-tautology` | all | SETTLED | Complete. At any k where best-known = bound - 1, *any* argument shaving one unit lands on best-known by construction of the table. T6's numeric match at 14/18/20 was empty. T7 caught it, T8 retracted outright rather than parking it. Correct on both sides. | T6 | T8 |
| `residue-stacking-cb-vs-improved-even` | all | DEAD | Retracted with its plan in T8. Whether Clement-Bader's mod-6 obstruction is orthogonal to the improved even-k bound is unknown to both agents; neither has the derivation and both said so. | T6 | T8 |
| `mod2-weak-filter` | 14 | SETTLED | Methodological, and arithmetically checkable: 28 even values survive in [0,54] versus 8 multiples of 7. T1 framed mirror parity as symmetric evidence to the C_7 kill; T2 called it, T3 conceded. Re-derived rigorously for f=0 in T11 and correctly dismissed again as vacuous in T12 — 54 was never odd. | T2 | T12 |
| `sat-not-run-at-k14` | 14 | SETTLED | Textual, and correct. The packet reports Savchuk's Kissat results for k=23, 27 and the k=11 unreachability only. It says nothing about 14/18/20 terminating with a verdict, and the table shows them OPEN. T10 conceded the read. Neither side may cite "the solver looked and failed" at these k. | T9 | T10 |
| `symmetry-tax-pattern` | 14 | CONTESTED | Euclidn't's central inductive claim: five independent structural taxes converging is what an obstruction looks like, not what insufficient search looks like. Referee's view: three of the five (C_7, central symmetry, f>0 mirror) are taxes on *specific symmetry groups* and say nothing about the asymmetric case, which is where any 54 would live. The pattern is real but its scope is narrower than the inference drawn from it. Not evidence about N(14) until someone taxes a family with no symmetry at all. | T15 | T20 |
| `k14-54-reachable` | 14 | CONTESTED | The actual question. After 26 turns: zero explicit lines, zero coordinates, zero triangle counts, zero verifier runs, by either side. Both win conditions are untouched. | T1 | T26 |
